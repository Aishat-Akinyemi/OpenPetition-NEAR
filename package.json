{
  "name": "1",
  "version": "1.0.0",
  "description": "decentralised Traded Funds\"",
  "main": "index.js",
  "scripts": {
    "dev": "watch -d -n 1 'clear && yarn test:unit'",
    "build": "yarn build:contract:debug",
    "build:contract": "asb",
    "build:contract:debug": "asb --target debug",
    "clean": "rm -rf ./out",
    "test": "yarn test:unit",
    "test:unit": "asp --verbose -f unit.spec"   
  },
  "author": "aishat-akinyemi",
  "license": "ISC",
  "devDependencies": {
    "near-sdk-as": "^3.2.3"
  }
}
